Dear <%= @order.name %>

Thank you for your recent order from Pragmatic Store.

You ordered the following item:

<%= render @order.line_items %>

We'll send you a separate e-mail when your order ship.